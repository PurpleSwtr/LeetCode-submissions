[project]
name = "leetcode"
version = "0.1.0"
description = "Hand-written LeetCode solutions plus an automatic sync of accepted submissions"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.1.1",
    "rich>=15.0.0",
]
